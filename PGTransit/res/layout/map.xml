<?xml version="1.0" encoding="utf-8"?>
    
<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <!-- The main content view -->

    <RelativeLayout
        android:id="@+id/mapFrame"
        android:layout_width="match_parent"
        android:layout_height="match_parent" >
        
        <!-- Map goes here -->
        
        <Button
            android:id="@+id/showDrawerButton"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:layout_alignParentRight="true"
            android:layout_marginRight="58dp"
            android:layout_marginTop="11dp"
            android:background="@color/google_maps_btn"
            android:contentDescription="@string/mapDrawerButton"
            android:drawableRight="@drawable/device_access_storage"
            android:text="@string/mapDrawerButton"
            android:textColor="#BB333333" />

    </RelativeLayout>
    <!-- The navigation drawer -->

    <ListView
        android:id="@+id/routesDrawer"
        android:layout_width="240dp"
        android:layout_height="wrap_content"
        android:layout_gravity="right"
        android:background="#111"
        android:choiceMode="singleChoice"
        android:divider="@android:color/transparent"
        android:dividerHeight="0dp" />

</android.support.v4.widget.DrawerLayout>